# Firmware (Raspberry Pi Zero W, real hardware backends)
RPi.GPIO==0.7.1
spidev==3.6
# Image rasterization stays deferred with the unselected e-paper panel.
# Pillow 12 dropped Bullseye's Python 3.9; do not add an incompatible pin.

# Firmware + shared
requests==2.32.3
defusedxml==0.7.1

# Web UI
Flask==3.0.3

# INDI client bindings (LGPL, used as a runtime dependency — see docs/CODES.md
# licensing note); not required for --simulate. Deliberately NOT listed here
# — see requirements-pyindi.lock, installed separately with --no-deps by
# install.sh/update.py, since its declared (but unused) bottle/dbus-python
# dependencies can't be built on the Pi Zero W.
