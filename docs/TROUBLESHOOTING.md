# Troubleshooting / bugfix manual — placeholder

Not written yet — depends on `firmware/ui/`, `install/install.sh`, and
`install/update.py`, none of which exist yet (see
[ARCHITECTURE.md → Remaining work](ARCHITECTURE.md#remaining-work-for-v0-handoff)).

See [CODES.md](CODES.md) for the maintainer-facing status/error code list
this manual should be organized around once written (`INDI-*`, `OTA-*`,
`WIFI-*`, `SEARCH-*`).
